services:
  - type: web
    name: streamlit-resume-builder
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app_working.py --server.port $PORT
    plan: free
